const menuBtn=document.getElementById('menuBtn');const navMenu=document.getElementById('navMenu');if(menuBtn){menuBtn.onclick=()=>navMenu.classList.toggle('open')}document.querySelectorAll('nav a').forEach(a=>a.onclick=()=>navMenu.classList.remove('open'));
const gen=document.getElementById('generate');if(gen){gen.onclick=()=>{const p=document.getElementById('prompt').value||'Customer has not entered a description yet.';const file=document.getElementById('file').files[0];document.getElementById('brief').textContent=`SHAPEFORGE 3D PROJECT BRIEF\n\nCustomer request:\n${p}\n\nReference file:\n${file?file.name:'No file uploaded'}\n\nRecommended workflow:\n1. Create or clean 3D model.\n2. Check wall thickness and scale.\n3. Export STL / OBJ / 3MF.\n4. Offer Print at Home download or We Print It quote.\n\nSuggested material:\nPETG for strong practical parts, PLA for simple display parts, Resin for fine detail.`}};
const quoteBtn=document.getElementById('quoteBtn');if(quoteBtn){quoteBtn.onclick=()=>{const grams=Number(document.getElementById('grams').value||0);const mat=document.getElementById('material').value;const q=document.getElementById('quality').value;let rate={PLA:.3,PETG:.36,ABS:.38,TPU:.48,Resin:.62,Nylon:.85}[mat]||.35;let mult=q==='Fine detail'?1.35:q==='Strong part'?1.25:1;let total=Math.max(18,grams*rate*mult+12);document.getElementById('quote').textContent=`Estimated quote: $${total.toFixed(2)}`}};
const camBtn=document.getElementById('startCamera');if(camBtn){camBtn.onclick=async()=>{const v=document.getElementById('camera');try{const stream=await navigator.mediaDevices.getUserMedia({video:{facingMode:'environment'},audio:false});v.srcObject=stream;v.style.display='block';camBtn.textContent='Camera Running'}catch(e){alert('Camera access was blocked or unavailable on this device.')}}}
if('serviceWorker' in navigator){navigator.serviceWorker.register('service-worker.js').catch(()=>{})}
